import { DirectivasPersonalizadasDirective } from './directivas-personalizadas.directive';

describe('DirectivasPersonalizadasDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivasPersonalizadasDirective();
    expect(directive).toBeTruthy();
  });
});
